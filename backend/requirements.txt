# Core poker engine
pokerkit>=0.7

# API server (fastapi-cli provides the `fastapi dev`/`fastapi run` commands;
# uvicorn is what actually runs underneath them)
fastapi>=0.115
fastapi-cli[standard]>=0.0.5
uvicorn[standard]>=0.32
websockets>=13.0
pydantic>=2.9
python-dotenv>=1.0

# AI providers (OpenAI-compatible client also covers DeepSeek and xAI/Grok)
anthropic>=0.40
openai>=1.55
google-genai>=0.3

# Local TTS (run `python scripts/download_tts_model.py` once to fetch model weights)
kokoro-onnx>=0.5

# Testing
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
